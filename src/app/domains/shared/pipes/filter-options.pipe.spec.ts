import { FilterOptionsPipe } from './filter-options.pipe';

describe('FilterOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
